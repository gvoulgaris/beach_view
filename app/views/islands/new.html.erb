<!-- Validation failure messages -->
<% if @island.errors.any? %>
  <% @island.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Island</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_island" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for island_name -->
      <div class="form-group">
        <label for="island_name" class="control-label">
          Island name
        </label>

        <input type="text" id="island_name" name="island_name" placeholder="Island name" class="form-control" value="<%= @island.island_name %>">
      </div>

      <!-- Label and input for location -->
      <div class="form-group">
        <label for="location" class="control-label">
          Location
        </label>

        <input type="text" id="location" name="location" placeholder="Location" class="form-control" value="<%= @island.location %>">
      </div>

      <button class="btn btn-success">
        Create Island
      </button>
      or
      <a href="/islands">Cancel</a>
    </form>
  </div>
</div>
